{
  "scripts": {
    "setup": "node setup",
    "dev": "node server",
    "dev-firebase": "firebase serve --config firebase.dev.json",
    "deploy": "firebase deploy --only hosting:codetmp  --config firebase.prod.json",
    "deploy-dev": "firebase deploy --only hosting:codetmp  --config firebase.dev.json",
    "build": "node build"
  },
  "dependencies": {
    "ace-builds": "1.32.7",
    "express": "4.19.1",
    "fs": "0.0.1-security",
    "fs-extra": "9.1.0",
    "html-minifier": "4.0.0",
    "minify": "6.0.1"
  }
}

{
  "scripts": {
    "dev": "firebase serve --only hosting:codetmp --config firebase.dev.json",
    "deploy": "firebase deploy --only hosting:codetmp  --config firebase.prod.json",
    "build": "node build"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "fs-extra": "^9.1.0",
    "html-minifier": "^4.0.0",
    "minify": "^6.0.1"
  }
}
